<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <p>ini route default dan hasil dari menggunakan sendFile</p>
    <p>silahkan coba akses url dibawah ini</p>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/about/">About</a></li>
      <li><a href="/contact">Contact</a></li>
      <!-- produk dengan id 1 -->
      <li><a href="/product/1">Product 1</a></li>
      <!-- produk dengan id 2 dengan category id 10 -->
      <li><a href="/product/2/category/10">Product 2 with category 10</a></li>
      <!-- variant dengan id 3 dengan query parameter name=John -->
      <li><a href="/variant/3?name=John">Variant 3 with name=John</a></li>
    </ul>

    <!-- menggunakan ejs untuk mengambil data -->
    <% const fakeName = "John" %>
    <h2>Nama palsu saya adalah <%= fakeName %></h2>

    <!-- ejs diambil dari app.js -->
    <h2>Nama asli saya adalah <%= name %></h2>

    <!-- Membuat daftar pejabat -->
    <h2>Daftar pejabat</h2>
    <!-- Cek apakah kosong menggunakan if else dan for each -->
    <% if (pejabat.length > 0) { %> <% pejabat.forEach((pjbt) => { %>
    <ul>
      <li><%= pjbt.nama %></li>
      <li><%= pjbt.email %></li>
    </ul>
    <% }) %> <% } else { %>
    <p>Belum ada pejabat</p>
    <% } %>
  </body>
</html>
